/**
 * Displays a message on the STDOUT.
 * @param {String} msg The message to display.
 * @author Chisom Ibekwe  <https://github.com/Lucasbolt>
 */
const displayMessage = (msg) => {
  console.log(msg);
};

module.exports = displayMessage;
